name: Build and push .NET packages

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0.x'

     # Add NuGet Sources

    - name: Create Local NuGet Directory
      run: mkdir ~/nuget

    - name: Add Local Nuget Source
      run: dotnet nuget add source ~/nuget

    - name: Add GGroupp NuGet Source
      run: >
        dotnet nuget add source ${{ secrets.GG_NUGET_SOURCE_URL }}
        -n ggroupp
        -u ${{ secrets.GG_NUGET_SOURCE_USER_NAME }}
        -p ${{ secrets.GG_NUGET_SOURCE_USER_PASSWORD }}
        --store-password-in-clear-text

    # CosmosApi.Entity.ItemPath.csproj

    - name: Restore CosmosApi.Entity.ItemPath.csproj
      run: dotnet restore ./src/*/*/CosmosApi.Entity.ItemPath.csproj

    - name: Build CosmosApi.Entity.ItemPath.csproj
      run: dotnet build ./src/*/*/CosmosApi.Entity.ItemPath.csproj --no-restore  -c Release

    - name: Pack CosmosApi.Entity.ItemPath.csproj
      run: dotnet pack ./src/*/*/CosmosApi.Entity.ItemPath.csproj --no-restore -o ~/nuget  -c Release

    # CosmosApi.Contract.Read.csproj

    - name: Restore CosmosApi.Contract.Read.csproj
      run: dotnet restore ./src/*/*/CosmosApi.Contract.Read.csproj

    - name: Build CosmosApi.Contract.Read.csproj
      run: dotnet build ./src/*/*/CosmosApi.Contract.Read.csproj --no-restore  -c Release

    - name: Pack CosmosApi.Contract.Read.csproj
      run: dotnet pack ./src/*/*/CosmosApi.Contract.Read.csproj --no-restore -o ~/nuget  -c Release

    # CosmosApi.Contract.Write.csproj

    - name: Restore CosmosApi.Contract.Write.csproj
      run: dotnet restore ./src/*/*/CosmosApi.Contract.Write.csproj

    - name: Build CosmosApi.Contract.Write.csproj
      run: dotnet build ./src/*/*/CosmosApi.Contract.Write.csproj --no-restore  -c Release

    - name: Pack CosmosApi.Contract.Write.csproj
      run: dotnet pack ./src/*/*/CosmosApi.Contract.Write.csproj --no-restore -o ~/nuget  -c Release

    # CosmosApi.Contract.Delete.csproj

    - name: Restore CosmosApi.Contract.Delete.csproj
      run: dotnet restore ./src/*/*/CosmosApi.Contract.Delete.csproj

    - name: Build CosmosApi.Contract.Delete.csproj
      run: dotnet build ./src/*/*/CosmosApi.Contract.Delete.csproj --no-restore  -c Release

    - name: Pack CosmosApi.Contract.Delete.csproj
      run: dotnet pack ./src/*/*/CosmosApi.Contract.Delete.csproj --no-restore -o ~/nuget  -c Release

    # CosmosApi.Contract.Lock.csproj

    - name: Restore CosmosApi.Contract.Lock.csproj
      run: dotnet restore ./src/*/*/CosmosApi.Contract.Lock.csproj

    - name: Build CosmosApi.Contract.Lock.csproj
      run: dotnet build ./src/*/*/CosmosApi.Contract.Lock.csproj --no-restore  -c Release

    - name: Pack CosmosApi.Contract.Lock.csproj
      run: dotnet pack ./src/*/*/CosmosApi.Contract.Lock.csproj --no-restore -o ~/nuget  -c Release

    # CosmosApi.Contract.csproj

    - name: Restore CosmosApi.Contract.csproj
      run: dotnet restore ./src/*/*/CosmosApi.Contract.csproj

    - name: Build CosmosApi.Contract.csproj
      run: dotnet build ./src/*/*/CosmosApi.Contract.csproj --no-restore  -c Release

    - name: Pack CosmosApi.Contract.csproj
      run: dotnet pack ./src/*/*/CosmosApi.Contract.csproj --no-restore -o ~/nuget  -c Release

    # CosmosApi.csproj

    - name: Restore CosmosApi.csproj
      run: dotnet restore ./src/*/*/CosmosApi.csproj

    - name: Build CosmosApi.csproj
      run: dotnet build ./src/*/*/CosmosApi.csproj --no-restore  -c Release

    - name: Pack CosmosApi.csproj
      run: dotnet pack ./src/*/*/CosmosApi.csproj --no-restore -o ~/nuget  -c Release

    # CosmosStorage.csproj

    - name: Restore CosmosStorage.csproj
      run: dotnet restore ./src/*/*/CosmosStorage.csproj

    - name: Build CosmosStorage.csproj
      run: dotnet build ./src/*/*/CosmosStorage.csproj --no-restore  -c Release

    - name: Pack CosmosStorage.csproj
      run: dotnet pack ./src/*/*/CosmosStorage.csproj --no-restore -o ~/nuget  -c Release

    # Push

    - name: Push Packages
      if: ${{ github.ref == 'refs/heads/main' }}
      run: >
        dotnet nuget push "../../../nuget/*.nupkg"
        -s ${{ secrets.GG_NUGET_SOURCE_URL }}
        -k ${{ secrets.GG_NUGET_SOURCE_USER_PASSWORD }}
        --skip-duplicate